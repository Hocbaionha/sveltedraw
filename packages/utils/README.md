# @sveltedraw/utils

Sveltedraw utility functions (export to PNG/SVG/Blob, bbox, withinBounds).

Forked from the upstream Excalidraw `@excalidraw/utils` package and adapted
for the Svelte port. Workspace-internal — not published to npm.
